<!DOCTYPE html>
<html>

<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title>{{ h.defaultPageTitle }}</title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>

<body>
    <div>{{ msg }}</div>
    <div class="d-flex align-items-center justify-content-center" id="corpo">
        <div class="card w-50 m-5">
            <div class="card-header bg-primary">
                <div class="text-light">Chamar paciente</div>
            </div>
            <div class="card-body">
                <div class="form-group">
                <form method="post" action="/screen" target="screenWindow" id="formTTS">
                    <label for="nomePaciente">Paciente</label>
                    <input type="text" id="nomePaciente" name="nomePaciente" class="form-control" required="true">
                    <label class="mt-2" for="consultorio">Consultório</label>
                    <select name="consultorio" id="consultorio" class="form-control">
                        <option value="consultorio 1">Consultório 1</option>
                        <option value="consultorio 2">Consultório 2</option>
                        <option value="consultorio 3">Consultório 3</option>
                        <option value="medicacao">Medicação</option>
                        <option value="eletro">Eletro</option>
                    </select>
                    <label class="mt-2" for="repetir">Repetir</label>
                    <select name="repetir" id="repetir" class="form-control">
                        <option value="1">1</option>
                        <option value="2">2</option>
                        <option value="3">3</option>
                    </select>
                   <button type="submit" class="mt-4 btn btn-primary btn-lg" id="btnFalar">Falar</button>
                </form>
                </div>
            </div>
            <div class="card-footer">
            </div>
            {{! <div class="card"> }}
                {{! <div class="card-header"> }}
                    {{! <div class="card-title">Fila</div> }}
                {{! </div> }}
                {{! <div class="card-body"> }}
                    {{! {{ queue }} 
                    {{! {{ queue }} 
                {{! </div> }}
                {{! <div class="card-footer"></div> }}
            </div>
        </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script type="text/javascript" src="https://unpkg.com/speech-synthesis-recorder@1.2.1/SpeechSynthesisRecorder.js"></script>
    <script src="./src/dist/js/home.js"></script>
</body>

</html>